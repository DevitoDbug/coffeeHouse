import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const BeanDetailScreen = (): JSX.Element => {
  return (
    <View>
      <Text>BeanDetailScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
