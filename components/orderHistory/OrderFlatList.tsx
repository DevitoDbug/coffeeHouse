import React from "react";
import { StyleSheet, Text, View } from "react-native";

export const OrderFlatList = (): JSX.Element => {
  return (
    <View>
      <Text>OrderFlatList</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
