import React from "react";
import { StyleSheet, Text, View } from "react-native";

export const OrderItem = (): JSX.Element => {
  return (
    <View>
      <Text>OrderItem</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
